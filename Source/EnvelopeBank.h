/*
  ==============================================================================

    EnvelopeBank.h
    Created: 16 Sep 2020 3:53:31pm
    Author:  gabey

  ==============================================================================
*/

/*#pragma once
#include "Envelope.h"
#include <vector>

class EnvelopeBank {
public:
	void addEnvelope();
	void addEnvelope(double freq);
	void removeEnvelope(int);
	void setFrequency(int, double);
	void addPoint(int envelope, double x, double y);
	void addPoint(int envelope, double x, double y, double skew);
	void movePoint(int envelope, double deltaTime, double deltaDecibels);



	std::vector<std::vector<double>> getPoints(int index);
	std::vector<std::vector<double>> getSkewPoints(int index);
	std::vector<std::vector<double>> getEdge(int index);


	void clear();
	int size();
private:
	std::vector<Envelope *> bank;
};*/